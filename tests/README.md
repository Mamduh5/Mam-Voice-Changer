# Test boundaries

Rust and frontend unit tests must not require a particular physical audio device. Real
WASAPI stream creation, VB-CABLE routing, acoustic monitoring, compatibility checks, and
long-duration stability belong in `docs/manual-test-plan.md`.
